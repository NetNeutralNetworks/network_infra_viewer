# TODO: i would like the ttp parte to be tested, every sub template should be tested indevidually like in teh example below

# result = ios.parse(os.path.join(BACKUP_FOLDER), os.path.join(DESTINATION_FOLDER,HOSTNAME+'.oc'), template = 'show_run_access_list.ttp')
